.jumbotron
  .container.text-center
    = image_tag("header_logo.png", alt: "PROTO TYPE - SHARE THE PROTOTYPE", height: "73px")

.filter-nav
  .container
    .row
    = render 'prototypes/nav'

.container.proto-list
  .row#proto

  = render @prototypes

.text-center
  %ul.pagination
    %li.disabled
      = paginate @prototypes
