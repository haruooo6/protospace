$('#comment_area').val('');
$('#comments').html(
  '<%= j(render partial: 'comments/comment', collection: @prototype.comments) %>'
);
$('span.num').html(
  "<%= j("#{@prototype.comments_count}") %>"
);
